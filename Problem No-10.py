# Write a python script to use IS operator to display if both variables are the same object or not?
a,b=int(input("Enter the first data : ")),int(input("Enter second data: "))
print(" Both are representing to same object!") if a is b else print("Both are representing to different objects!")